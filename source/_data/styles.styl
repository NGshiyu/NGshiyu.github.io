// 自定义样式文件

// 添加一些自定义Stylus样式
body
  // 确保canvas背景不会影响其他元素
  canvas
    display block

  // 为页面添加平滑过渡效果
  transition all 0.3s ease

// 鼠标悬停时的发光效果
//.post-body:hover,
//.sidebar:hover,
//.header:hover
//  box-shadow 0 0 20px rgba(52, 152, 219, 0.3)
//  transition box-shadow 0.3s ease

// 自定义滚动条样式
::-webkit-scrollbar
  width 8px

::-webkit-scrollbar-track
  background rgba(0, 0, 0, 0.1)

::-webkit-scrollbar-thumb
  background linear-gradient(45deg, #3498db, #9b59b6)
  border-radius 4px

::-webkit-scrollbar-thumb:hover
  background linear-gradient(45deg, #2980b9, #8e44ad)

// 中文段落首行缩进
.post-body p,
.post-detail p,
.post-content p
  text-indent 2em
  margin 0
  padding 0

// 段落间距调整 - 移除段落间的空行间隔
.post-body p + p,
.post-detail p + p,
.post-content p + p
  margin-top 0

// 针对列表、引用等特殊元素取消缩进
.post-body ul li,
.post-body ol li,
.post-body blockquote p,
.post-detail ul li,
.post-detail ol li,
.post-detail blockquote p
  text-indent 0

// 标题不缩进
.post-body h1, .post-body h2, .post-body h3, .post-body h4, .post-body h5, .post-body h6
  text-indent 0

// =============================================================================
// GitHub卡片样式模块 - Import方式
// =============================================================================
// 使用@import引入独立的GitHub卡片样式文件
// 所有GitHub卡片相关的样式都在对应的文件中管理

@import 'gr_card_style.styl'
